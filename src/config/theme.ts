import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#003ca0',
            contrastText: '#fff',
        },
        secondary: {
            main: '#3364b3',
        },
    },
});

export default theme;
